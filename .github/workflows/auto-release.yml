name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency: auto-release-${{ github.ref }}

jobs:
  release:
    name: Release
    uses: tyler-technologies/forge-automation-shared/.github/workflows/wf-build-release.yml@v0.11.0
    with:
      BUILD_ENABLED: false
    secrets:
      NPM_TOKEN: ${{ secrets.FORGE_NPM_TOKEN }}
